---
plugin: aws_ec2
regions:
  - eu-west-3
# hostnames:
#   - ip-address
include_filters:
# This includes everything in the inventory that has the following tags.
# - tag:DB:   #Format Multiple Tags Use.
#     - 'pg'
- tag:type:   
    - 'logstash'